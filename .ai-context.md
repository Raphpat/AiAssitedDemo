# Project Context

## Project Structure

- Backend: Gradle-based Java Spring Boot project with math utilities (Java 21)
- Frontend: Angular application with modern styling system

## Project Organization

### Backend

- `src/main/java/com/example/`
  - `MathUtils.java`: Core math operations
  - `MathApplication.java`: Spring Boot application with CORS configuration
  - `MathController.java`: REST endpoints

### Frontend

- `frontend/src/app/`
  - `components/`
    - `calculator/`: Calculator component
    - `prime-checker/`: Prime number checker component
  - `services/`
    - `math.service.ts`: Service for backend API communication

## TypeScript & Angular Standards

- Use strict TypeScript configuration
- Prefer type inference over explicit typing where possible
- Use modern ES features (optional chaining, nullish coalescing, etc.)
- Implement strict null checks
- Use interface segregation for better type organization
- Leverage const assertions and literal types
- Use discriminated unions for complex state management
- Prefer readonly arrays and objects where immutability is desired
- Use template literal types for string manipulation
- Implement proper type guards and type narrowing
- Use private class fields with # prefix where appropriate

## Configuration

### VS Code Launch Configurations

- "MathApplication": Launches Spring Boot backend on port 8080
- "Launch Frontend": Starts Angular dev server on port 4200
- "Launch Full Stack": Compound launch for both frontend and backend

### Backend Configuration

- Spring Boot 3.2.3
- Java 21
- Gradle build system
- CORS enabled for frontend

### Frontend Configuration

- Angular standalone components
- Reactive forms
- HTTP client for API communication
- Strict TypeScript configuration
- Modern ESNext features enabled

## API Endpoints

- POST /api/math/add
- POST /api/math/multiply
- POST /api/math/divide
- GET /api/math/isPrime

## Environment Notes

- Backend runs on port 8080
- Frontend runs on port 4200
- CORS configured for localhost development

## Styling System

### Global Styles Organization

- `_variables.scss`: Contains all design tokens

  - Color palette with primary, secondary, success, error, and neutral colors
  - Semantic colors for text and backgrounds
  - Spacing scale (xs to xxl)
  - Typography settings (font families, sizes, weights)
  - Breakpoints, border-radius, transitions, z-indices

- `_typography.scss`: Typography configuration

  - Imports Inter (base) and Fira Code (monospace) fonts
  - Sets root font settings with antialiasing
  - Handles font imports and base styles

- `_mixins.scss`: Reusable style patterns
  - Color getter functions
  - Typography mixins
  - Responsive mixins
  - Common UI patterns (input-base, button-base, card)
  - Container and loading spinner mixins

### Recent Style Improvements

1. **Layout & Container**

   - Added container mixin for consistent width and padding
   - Responsive padding adjustments for different screen sizes

2. **Typography**

   - Added Inter as base font
   - Added Fira Code as monospace font for inputs
   - Improved font smoothing and rendering

3. **Component Styling**

   - Floating animation for grid items
   - Desynchronized animations for natural feel
   - Gradient background using palette colors
   - Card shadows and hover effects

4. **Input Fields**
   - Custom monospace font styling
   - Consistent padding and borders
   - Focus and error states

## Implementation Progress

- Backend: Complete with Java 21
- Backend: Complete
- Frontend: In progress
  - Calculator component: Complete
  - Prime checker component: Complete
  - Styling system: Complete

## TODO

1. Complete Java 21 upgrade
2. Fix any Angular dependency issues
3. Create remaining REST endpoints
4. Monitor font loading and styling issues

## API Endpoints

- GET /api/math/calculate
- GET /api/math/prime-check

## Git Branches

- ai_example_dev
- feat_ai-1
- feat_ai-5
